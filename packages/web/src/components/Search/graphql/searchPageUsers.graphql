query SearchPageUsers($query: String!, $offset: Int! $limit: Int!) {
    usersByQuery(query: $query, offset: $offset, limit: $limit) {
        id
        name
        nickname
        birthDate
        city
        image {
            id
        }
    }
}